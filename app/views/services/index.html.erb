<h1><%= t("services.tablename") %></h1>

<table>
  <tr>
    <th><%= t("services.title") %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @services.each do |service| %>
  <tr>
    <td><%= service.title %></td>
    <td><%= show service %></td>
    <td><%= edit service %></td>
    <td><%= delete service %></td>
  </tr>
<% end %>
</table>

<br />

<%= new %>