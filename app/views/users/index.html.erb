<h1><%= t("users.tablename") %></h1>

<table>
  <tr>
    <th><%= t("users.user") %></th>
    <th><%= t("users.define_id") %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.user %></td>
    <td><%= user.define.name %></td>
    <td><%= show user %></td>
    <td><%= edit user %></td>
    <td><%= delete user %></td>
  </tr>
<% end %>
</table>

<br />

<%= new %>
