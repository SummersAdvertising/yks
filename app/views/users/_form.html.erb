<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user, '帳號' %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :password, '密碼' %><br />
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :define_id, '類型' %><br />
	<%= f.select :define_id, "<option value=\"1\">管理員</option><option value=\"2\">一般員工</option>", :prompt => t("helpers.form.select.prompt")%>
  </div>
  <div class="actions">
    <%= submit %>
    <%= reset %>
  </div>
<% end %>
