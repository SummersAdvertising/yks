<h1><%= t("news.tablename") %></h1>

<table>
  <tr>
    <th><%= t("news.title") %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @news.each do |new| %>
  <tr>
    <td><%= new.title %></td>
    <td><%= show new %></td>
    <td><%= edit new %></td>
    <td><%= delete new %></td>
  </tr>
<% end %>
</table>

<br />

<%= new %>
