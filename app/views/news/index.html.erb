<h1>Listing services</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @news.each do |new| %>
  <tr>
    <td><%= new.id %></td>
    <td><%= new.title %></td>
    <td><%= link_to 'Show', new %></td>
    <td><%= link_to 'Edit', edit_news_path(new) %></td>
    <td><%= link_to 'Destroy', new, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>


<br />

<%= link_to 'New Service', new_news_path %>
